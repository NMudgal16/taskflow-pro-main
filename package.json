{
  "name": "team-task-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Team Task Manager with React, Express, MongoDB, and JWT auth.",
  "scripts": {
    "start": "npm --prefix backend start",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend start\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend start",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "install:all": "npm install",
    "postinstall": "npm --prefix backend install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@react-three/fiber": "^8.17.10",
    "mongodb": "^7.2.0"
  }
}
